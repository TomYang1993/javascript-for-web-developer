// exec() find information about the matched value

// test() return boolean for pattern match

// pattern has properties, mainly around how it's formatted

// REGEXP has properties, after exec(), test(), info about how the pattern match going