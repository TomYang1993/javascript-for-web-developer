var date = new Date();
console.log(date);

// Date.parse() Date.UTC() returns milliseconds format on specific date input
// Date.now() provides milliseconds format on current time

// toString valueOf methods are overwritten, valueOf returns milliseconds, makes it possible for number comparison

